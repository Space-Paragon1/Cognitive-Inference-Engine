# Leave empty to use Vite's dev proxy (points to http://127.0.0.1:8765 locally).
# Set to your Railway URL to connect directly to the hosted backend.
# Example: https://cognitive-inference-engine-production.up.railway.app
VITE_API_URL=
